Bạn kiểm tra cho tui 2 file là run_swat.py và run_wadi.py và các file liên quan 2 file đó xem đã chạy 2 stage đều là resnet hết chưa. 
Có một số phần khi chạy swat và wadi đã được xuống cpu đúng chưa.
Mọi thứ đã ổn hết để chạy trên kaggle GPU T4 hết rồi đúng ko?